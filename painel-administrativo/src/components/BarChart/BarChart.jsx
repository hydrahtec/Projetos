import {useTheme} from '@mui/material';
import { ResponsiveBar } from '@nivo/bar';
import {tokens} from '';
import {mockBarData as data} from '';

export const BarChart = ({isDashboard = false}) => {
    const theme = useTheme();

};